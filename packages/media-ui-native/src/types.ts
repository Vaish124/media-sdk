// STUB: React Native implementation is intentionally out of scope.
// TypeScript signatures match @media-sdk/media-ui-react exactly.
// See README.md → Scoping Decisions for rationale.
// @platform react-native

// Minimal structural stand-ins for `react-native` types. The real `react-native`
// package is not installed in this repo (see README.md → Scoping Decisions), so
// these approximate the shapes a real implementation would use.
export type GestureResponderEvent = { nativeEvent: unknown };
export type NativeViewHandle = unknown;
